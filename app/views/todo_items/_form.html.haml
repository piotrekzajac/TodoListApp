- if @todo_item.errors.any?
  #error_explanation
    %h2
      = pluralize(@todo_item.errors.count, "error")
      prohibited this todo item from being saved:

    %ul
      - @todo_item.errors.full_messages.each do |message|
        %li= message

= form.label :content
= form.text_field :content

= form.submit 'Save'